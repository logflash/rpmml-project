### Add these to your env:

`pip install typed-argument-parser`
`pip install GitPython`
`pip install tqdm`
`pip install "minari[hf]"`
`pip install "minari[hdf5]"`

Test rendering capabilities with:
`python video_traj.py`