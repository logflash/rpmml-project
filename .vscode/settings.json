{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\.(py)$",
        "cmd": "black ${file}"
      },
      {
        "match": "\\.(py)$",
        "cmd": "isort ${file}"
      }
    ]
  }
}